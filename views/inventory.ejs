<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Cold Container - Inventory</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&family=Pacifico&display=swap');
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #f5f7fa 0%, #e8f5e9 100%);
      position: relative;
    }
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-image: 
        url("data:image/svg+xml,%3Csvg width='80' height='80' viewBox='0 0 80 80' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%2366bb6a' fill-opacity='0.03'%3E%3Cpath d='M0 0h80v80H0V0zm20 20v40h40V20H20zm2 2h36v36H22V22z'/%3E%3C/g%3E%3C/svg%3E"),
        radial-gradient(circle at 10% 20%, rgba(102, 187, 106, 0.05) 0%, transparent 50%),
        radial-gradient(circle at 90% 80%, rgba(102, 187, 106, 0.05) 0%, transparent 50%);
      background-size: 80px 80px, 100% 100%, 100% 100%;
      pointer-events: none;
      z-index: 0;
    }
    body::after {
      content: '🍃';
      position: fixed;
      font-size: 200px;
      opacity: 0.02;
      top: 10%;
      right: 5%;
      pointer-events: none;
      z-index: 0;
      transform: rotate(-15deg);
    }
    .container {
      position: relative;
      z-index: 1;
    }
    nav {
      position: relative;
      z-index: 2;
    }
    .navbar-brand {
      font-family: 'Pacifico', cursive;
      font-size: 1.8rem !important;
      font-weight: 400;
    }
    .navbar {
      background-color: #6f9269 !important;
    }
    .btn-primary {
      background-color: #66bb6a;
      border-color: #66bb6a;
    }
    .btn-primary:hover {
      background-color: #5a9d4d;
      border-color: #5a9d4d;
    }
    .btn-outline-primary {
      color: #66bb6a;
      border-color: #66bb6a;
    }
    .btn-outline-primary:hover {
      background-color: #66bb6a;
      border-color: #66bb6a;
      color: white;
    }
    .btn-outline-danger {
      color: #c62828;
      border-color: #c62828;
    }
    .btn-outline-danger:hover {
      background-color: #c62828;
      border-color: #c62828;
      color: white;
    }
    .inventory-container {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 15px;
      padding: 2rem;
      box-shadow: 0 4px 15px rgba(102, 187, 106, 0.15);
      margin-top: 2rem;
    }
    table {
      border-color: #e8f5e9;
      background: white;
    }
    thead {
      background: linear-gradient(135deg, #76a279 0%, #66bb6a 100%);
      color: white;
    }
    thead th {
      border: none;
      padding: 1rem;
    }
    tbody tr {
      transition: background-color 0.2s;
    }
    tbody tr:hover {
      background-color: #e8f5e9;
    }
    .page-header {
      background: linear-gradient(135deg, #76a279 0%, #66bb6a 100%);
      color: white;
      padding: 2rem;
      border-radius: 10px;
      margin-bottom: 2rem;
      box-shadow: 0 4px 10px rgba(102, 187, 106, 0.2);
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fas fa-store me-2"></i>
        Cold Container
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">
              <i class="fas fa-home me-1"></i> Home
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/addProduct">
              <i class="fas fa-plus-circle me-1"></i> Add Product
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/orders">
              <i class="fas fa-receipt me-1"></i> Orders
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">
              <i class="fas fa-sign-out-alt me-1"></i> Logout
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container py-4">
    <div class="page-header">
      <h1 class="mb-0">
        <i class="fas fa-boxes me-2"></i>
        Inventory Management
      </h1>
      <p class="mb-0 mt-2">Manage your product catalog</p>
    </div>

    <div class="inventory-container">
      <% if (products && products.length) { %>
        <div class="table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>ID</th>
                <th>Product Name</th>
                <th>Price</th>
                <th>Stock</th>
                <th class="text-end">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% products.forEach(function(p) { %>
                <tr>
                  <td><strong>#<%= p.productId %></strong></td>
                  <td><%= p.productName %></td>
                  <td><strong>$<%= (Number(p.price) || 0).toFixed(2) %></strong></td>
                  <td>
                    <% if (p.quantity > 10) { %>
                      <span class="badge bg-success"><%= p.quantity %> units</span>
                    <% } else if (p.quantity > 0) { %>
                      <span class="badge bg-warning text-dark"><%= p.quantity %> units</span>
                    <% } else { %>
                      <span class="badge bg-danger">Out of Stock</span>
                    <% } %>
                  </td>
                  <td class="text-end">
                    <a href="/updateProduct/<%= p.productId %>" class="btn btn-sm btn-outline-primary">
                      <i class="fas fa-edit me-1"></i>Edit
                    </a>
                    <a href="/deleteProduct/<%= p.productId %>" class="btn btn-sm btn-outline-danger" onclick="return confirm('Delete this product?')">
                      <i class="fas fa-trash me-1"></i>Delete
                    </a>
                  </td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } else { %>
        <div class="text-center py-5">
          <i class="fas fa-box-open fa-3x text-muted mb-3"></i>
          <h4 class="text-muted">No products in inventory</h4>
          <p class="text-muted">Add your first product to get started</p>
          <a href="/addProduct" class="btn btn-primary mt-3">
            <i class="fas fa-plus me-2"></i>Add Product
          </a>
        </div>
      <% } %>
    </div>
  </div>

  <%- include('partials/cartButton') %>
</body>
</html>
