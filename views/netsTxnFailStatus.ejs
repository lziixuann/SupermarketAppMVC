<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed - NETS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body { background: #f5f5f5; padding: 2rem; }
        .fail-container { max-width: 600px; margin: 2rem auto; background: #fff; padding: 2rem; border-radius: 10px; box-shadow: 0 6px 20px rgba(0,0,0,0.08); text-align: center; }
        .fail-icon { font-size: 4rem; color: #dc3545; margin-bottom: 1rem; }
        .error-details { background: #fff3cd; padding: 1rem; border-radius: 8px; margin: 1.5rem 0; border-left: 4px solid #dc3545; text-align: left; }
    </style>
</head>
<body>
    <div class="fail-container">
        <div class="fail-icon">
            <i class="fas fa-times-circle"></i>
        </div>
        <h1 class="mb-3">Payment Failed</h1>
        <p class="text-muted mb-4">Unfortunately, your NETS payment could not be processed.</p>
        
        <div class="error-details">
            <strong>Error:</strong>
            <p class="mb-0"><%= errorMsg || 'Transaction failed. Please try again.' %></p>
            <% if (responseCode) { %>
            <small class="text-muted">Response Code: <%= responseCode %></small>
            <% } %>
        </div>

        <p class="text-muted">Please try again or use a different payment method.</p>
        
        <div class="mt-4">
            <a href="/checkout" class="btn btn-primary me-2">
                <i class="fas fa-redo"></i> Try Again
            </a>
            <a href="/cart" class="btn btn-outline-secondary">
                <i class="fas fa-shopping-cart"></i> Back to Cart
            </a>
            <a href="/" class="btn btn-outline-secondary">
                <i class="fas fa-home"></i> Home
            </a>
        </div>
    </div>
</body>
</html>